en:
  models:
    post:
      single_like: "%{name} liked your post"
      multiple_likes: "%{name} and %{count} other liked your post"
      minutes_ago: "%{count} minutes ago"
      hours_ago: "%{count} hours ago"
      now: "now"

  activerecord:
    models:
      user: "User"
    attributes:
      user:
        email: "Email"
        password: "Password"
        name: "Name"
      post:
        images: "Image"
    errors:
      models:
        user:
          attributes:
            name:
              invalid_format: "can only contain letters, digits, and underscores"
        post:
          attributes:
            images:
              blank: "must be selected"
              too_short: "must have at least 1"
              too_long: "can have up to 3"

{
  "name": "app",
  "private": true,
  "devDependencies": {
    "@types/node": "^24.9.2",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "esbuild app/javascript/src/index.ts --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets"
  },
  "dependencies": {
    "@hotwired/turbo-rails": "^8.0.20"
  }
}
